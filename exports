export PATH="$PATH:$HOME/.rvm/bin"
source $(brew --prefix nvm)/nvm.sh
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

# ÑAPA provisional arreglar en cuanto tenga tiempo.
export PATH="$PATH:$NVM_DIR/versions/node/v6.10.2/bin"

#My own bin folder
export VIROIDE_HOME="$HOME/dotfiles"
export PATH=$VIROIDE_HOME/bin:$PATH

export PATH="/usr/local/opt/ansible@2.0/bin:$PATH"
